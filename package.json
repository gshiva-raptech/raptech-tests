{
  "name": "raptech-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright UI automation for the migrated Raptech application (Item Master)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:item-master": "playwright test item-master",
    "report": "playwright show-report",
    "codegen": "playwright codegen $BASE_URL"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@types/node": "^20.0.0",
    "dotenv": "^16.4.5"
  }
}
